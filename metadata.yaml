file: script.sh
ame: restart_dns_default_pods
description: Restart dns pods in openshift-dns namespace
author: Yuri Diakov
allowedGroups:
  - SREP
  - LPSRE
  - CEE
rbac:
 roles:
   - namespace: "openshift-dns"
     rules:
       - verbs:
         - "get"
         - "patch"
         - "list"
         - "watch"
         apiGroups:
         - "apps"
         resources:
         - "daemonsets"
         - "pods"
envs:          
  - key: NAMESPACE
    description: The openshift-dns namespace 
    optional: false
language: bash
